<p id="notice"><%= notice %></p>

<h1>Movies</h1>

<table>
  <thead>
    <tr>
      <th>Api</th>
      <th>Title</th>
      <th>Vote average</th>
      <th>Release date</th>
      <th>Overview</th>
      <th>Poster path</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.api_id %></td>
        <td><%= movie.title %></td>
        <td><%= movie.vote_average %></td>
        <td><%= movie.release_date %></td>
        <td><%= movie.overview %></td>
        <td><%= movie.poster_path %></td>
        <td><%= link_to 'Show', movie %></td>
        <td><%= link_to 'Edit', edit_movie_path(movie) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Movie', new_query_path %>
