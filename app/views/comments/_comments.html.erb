<div class="row">
  <div class="col s12">
    <h4>Commenti:</h4>
    <ul class="collection" id="comments" data-channel="comments" data-movie-id="<%= movie.id %>">
      <%= render movie.comments %>
    </ul>

    <%= render 'comments/new', movie: movie %>
  </div>
</div>
